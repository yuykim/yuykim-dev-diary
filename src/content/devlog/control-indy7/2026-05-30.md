---
title: Control Indy7 연결
date: '2026-05-30'
type: diary
project: Control Indy7
project_slug: control-indy7
mood: connected
tags:
  - robotics
  - indy7
  - teleoperation
  - python
visibility: public
source_repo: yuykim/control_indy7
source_commit: 65fe92e286848dba3975ed8256667a3799d77cbf
---
# 시작점

Neuromeka Indy7 로봇팔을 Python과 Pygame으로 키보드 제어하기 위한 프로젝트를 Dev Diary에 연결했다.

이 repo는 로봇 상태 확인, 에러 복구, 홈 포지션 이동, 6자유도 키보드 teleoperation 흐름을 정리한다. 블로그에는 하드웨어 세팅 자체보다 제어 코드 구조, 안전하게 조작하기 위한 흐름, 실험 중 생긴 문제를 중심으로 기록한다.

## 기록할 내용

- Indy7 연결 확인과 실행 전 체크
- Pygame 기반 키 입력 처리 구조
- 위치/회전 제어 키맵 변경
- 에러 복구와 종료 루틴
- 실험 중 발견한 조작 안정성 문제

## 지금까지 한 작업 요약

### 로봇 구동 전 확인

- `indy7_start.py`로 로봇 연결, 서보 활성화, 상태 확인 흐름을 만들었다.

### 복구와 종료 루틴

- `error.py`로 teleop 중단, recover, servo on, 에러 상태 확인을 묶은 복구 루틴을 만들었다.
- `indy7_shutdown.py`로 home 복귀, motion stop, servo off를 수행하는 종료 루틴을 만들었다.

### 키보드 teleoperation

- `indy7_keyboard_control_v1.py`에서 Pygame 입력 루프와 `IndyDCP3` 상대 task teleop을 연결했다.
- 키보드로 X/Y/Z 이동과 RX/RY/RZ 회전을 제어하고, `[`/`]`로 step size를 조절하게 했다.
- `SPACE`로 home 위치에 복귀한 뒤 다시 teleop을 이어갈 수 있게 했다.
- 연속 입력이 부드럽게 들어가도록 약 30Hz 입력 루프를 사용했다.

### 진단과 컨트롤러 실험

- `check_teleop.py`와 `check_controller.py`로 teleop 상태와 외부 컨트롤러 입력을 진단했다.

### 문서화

- `readme.md`에 네트워크 설정, 실행 방법, 키맵, 안전 주의사항, 참고 이미지를 정리했다.

## 블로그 연결 작업

- 저장소를 워크스페이스 개인 프로젝트 영역인 `personal/robot-control-indy7`로 옮겼다.
- GitHub 원격은 `yuykim/control_indy7`를 `origin`으로 사용하고, 기존 조직 저장소는 `upstream`으로 보존했다.
- `.devlog.yml`, `HISTORY.md`, `dev_diary/`, `.github/workflows/publish-devlog.yml`를 추가해 Dev Diary 자동 반영 흐름을 붙였다.
- 이후 `HISTORY.md`나 `dev_diary/**`가 바뀌면 push 시 블로그에 자동으로 반영된다.
