name: Receive Dev Diary

on:
  repository_dispatch:
    types:
      - devlog_updated

concurrency:
  group: receive-devlog
  cancel-in-progress: false

jobs:
  receive:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout blog repository
        uses: actions/checkout@v4

      - name: Checkout source repository
        uses: actions/checkout@v4
        with:
          repository: ${{ github.event.client_payload.source_repo }}
          ref: ${{ github.event.client_payload.source_sha }}
          path: source-repo
          fetch-depth: 1

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Import dev diary
        run: |
          node scripts/import-devlog.js \
            --source ./source-repo \
            --repo "${{ github.event.client_payload.source_repo }}" \
            --sha "${{ github.event.client_payload.source_sha }}"

      - name: Commit imported content
        run: |
          git config user.name "devlog-bot"
          git config user.email "devlog-bot@users.noreply.github.com"
          git add src/content public/images
          git diff --staged --quiet && echo "No changes" || git commit -m "docs: import dev diary from ${{ github.event.client_payload.source_repo }}"
          git pull --rebase origin main
          git push
